## The path details will be deleted , reservation as well
## Remove  
 <!-- if (response?.error) {
        showPlatformAlert("Error", response.error);
        setError(response.error);
        return;
      } -->
